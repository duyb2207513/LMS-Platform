import { createHmac, timingSafeEqual } from "node:crypto"; import type { Request, Response } from "express"; import { AppError } from "../../common/errors/AppError.js"; import { sendSuccess } from "../../common/utils/response.js"; import { env } from "../../config/env.js"; import { completeMockPayment, getMockPayment, initiateMockPayment, processMockWebhook } from "./payments.service.js"; import type { MockCallbackInput, MockWebhookInput } from "./payments.types.js";
const origin = (request: Request) => `${request.protocol}://${request.get("host")}`; const p = (request: Request, key: string) => String(request.params[key] ?? "");
export async function initiateMockPaymentController(request: Request, response: Response) { sendSuccess(response, 201, "Mock payment initiated successfully", await initiateMockPayment(p(request, "orderId"), request.auth.userId, origin(request))); }
export async function mockCheckoutController(request: Request, response: Response) { const payment = await getMockPayment(p(request, "paymentId"), String(request.query.token ?? "")); const token = encodeURIComponent(payment.idempotencyKey); const action = `/api/v1/payments/mock/${payment.id}/callback`; response.type("html").send(`<!doctype html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width"><title>Mock Payment</title><style>body{font-family:Arial;max-width:520px;margin:50px auto;padding:24px;color:#17223b}.card{border:1px solid #ddd;border-radius:18px;padding:24px}button{border:0;border-radius:12px;padding:14px 20px;margin:8px;font-weight:700}.ok{background:#6335f5;color:#fff}.fail{background:#eee}</style></head><body><div class="card"><h1>Mock Payment</h1><p>Order: ${payment.order.orderNumber}</p><h2>${Number(payment.amount).toLocaleString("vi-VN")} ${payment.currency}</h2><form method="post" action="${action}"><input type="hidden" name="token" value="${token}"><button class="ok" name="status" value="SUCCEEDED">Thanh toán thành công</button><button class="fail" name="status" value="FAILED">Thanh toán thất bại</button></form></div></body></html>`); }
export async function mockCallbackController(request: Request, response: Response) { const result = await completeMockPayment(p(request, "paymentId"), request.body as MockCallbackInput); sendSuccess(response, 200, result.payment.status === "SUCCEEDED" ? "Payment completed successfully" : "Payment failed", result); }
export async function mockWebhookController(request: Request, response: Response) { const received = String(request.headers["x-mock-signature"] ?? ""); const expected = createHmac("sha256", env.mockPaymentWebhookSecret).update(JSON.stringify(request.body)).digest("hex"); const a = Buffer.from(received), b = Buffer.from(expected); if (a.length !== b.length || !timingSafeEqual(a, b)) throw new AppError(401, "Invalid webhook signature"); sendSuccess(response, 200, "Webhook processed successfully", await processMockWebhook(request.body as MockWebhookInput)); }
