<script setup lang="ts">import { computed } from 'vue';import DefaultLayout from '@/layouts/DefaultLayout.vue';import type { QuizResult } from '@/types';const result=computed(()=>{try{return JSON.parse(sessionStorage.getItem('quizResult')||'null') as QuizResult|null}catch{return null}})</script>
<template><DefaultLayout><main class="mx-auto max-w-2xl px-4 py-12"><div v-if="result" class="rounded-3xl border bg-white p-8 text-center dark:border-slate-800 dark:bg-slate-900"><div class="text-6xl">{{ result.passed?'🎉':'📚' }}</div><h1 class="mt-4 text-3xl font-extrabold">{{ result.passed?'Bạn đã vượt qua!':'Chưa đạt lần này' }}</h1><p class="my-5 text-5xl font-black text-purple-600">{{ result.score }}%</p><p>{{ result.earnedPoints }}/{{ result.totalPoints }} điểm · Lần làm {{ result.attemptNumber }}</p><div class="mt-8 flex justify-center gap-3"><RouterLink to="/my-courses" class="rounded-xl bg-purple-600 px-5 py-3 font-bold text-white">Khóa học của tôi</RouterLink><button class="rounded-xl border px-5 py-3" @click="$router.back()">Quay lại quiz</button></div></div><p v-else class="text-center">Không tìm thấy kết quả gần nhất.</p></main></DefaultLayout></template>
